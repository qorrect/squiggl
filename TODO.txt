Make this tool a npm global install that looks for a file named squigg.js and generates dao in a path
/squigg/out with a sample file and a package.json file

Add proper type definitions that will work with vanilla node